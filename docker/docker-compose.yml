version: '3.7'
services:
    game-server:
        build: ./images/debian-buster
        volumes:
            - ../project:/project
        ports:
            - 5000:5000
            - 8080:8080
            - 9222:9222
        working_dir: /project
        command: tail -f /dev/null
