syntax = "proto3";

package multiplayer;

import "input-action.proto";
import "spawn-request.proto";

message RequestRoot {
    int32 requestId = 1;
    uint32 requestSentTimestamp = 2;
    oneof message {
        InputAction inputAction = 3;
        SpawnRequest spawnRequest = 4;
    }
}
