syntax = "proto3";

package multiplayer;

import "input-action.proto";
import "spawn-request.proto";

message RequestRoot {
    int32 requestId = 1;
    oneof message {
        InputAction inputAction = 2;
        SpawnRequest spawnRequest = 3;
    }
}
