syntax = "proto3";

package multiplayer;

message InputAction {
    int32 frameIndex = 1;
    float yaw = 2;
    float pitch = 3;
    float rotationSpeed = 4;
    float rollAngle = 5;
    string objectId = 6; // this set by gateway app and not passed by client
}
